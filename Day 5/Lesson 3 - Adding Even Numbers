target = int(input()) # Enter a number between 0 and 1000
# 🚨 Do not change the code above ☝️

# Write your code here 👇
total = 0
# We need to cap it at the input given
for number in range(2, target + 1, 2):
  # Sum
  if number % 2 == 0:
    total += number
print(total)
  
